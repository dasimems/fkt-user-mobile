import { blackColor } from "@/assets/colors";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputStyle: {
    borderColor: blackColor.opacity50,
    backgroundColor: blackColor.opacity50
  }
});

export default styles;
